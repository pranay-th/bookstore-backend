"""
authors/admin.py

The Author model is an unused placeholder — the site's authors are derived
from the Book.author field, not this table. It is intentionally NOT registered
in the admin to avoid confusion. To manage authors, use the Books admin and
edit a book's `author` field.
"""
# (Author intentionally not registered — manage authors via Books.)
