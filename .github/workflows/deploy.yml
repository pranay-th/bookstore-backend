name: Backend Deploy

on:
  push:
    branches: [main]

jobs:
  deploy:
    name: Deploy to Render
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # TODO: Add Render deploy hook
      # TODO: Add RENDER_DEPLOY_HOOK_URL secret in GitHub repository settings
      - name: Placeholder deploy step
        run: echo "TODO - wire up Render deploy hook secret"
